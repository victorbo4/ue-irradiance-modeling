# --------------------
# Paper (lives in a separate Codeberg repo; kept here only as a working copy)
# --------------------
/paper/

# Thesis PDF — kept locally, not re-tracked (already in git history; superseded by the paper)
/TFG_*.pdf

# --------------------
# Unreal Engine
# --------------------
Binaries/
DerivedDataCache/
Intermediate/
Saved/
Build/
.vscode/

# Ignore config files that are user-specific
*.user
*.userprefs
*.suo
*.sln.docstates

# Ignore project-specific VS files
.vs/
*.VC.db
*.VC.opendb
*.opensdf

# Compiled source
*.dll
*.exe
*.lib
*.app
*.dylib
*.obj
*.o
*.pdb

# Ignore macOS and Linux system files
.DS_Store
*.swp
*.tmp

# Crash reports
*.log
*.dmp

# Generated docs (regenerate: doxygen Doxyfile from simulation/Plugins/Pyrano/)
simulation/Docs/html/
simulation/Docs/latex/

# Editor / tooling generated
.ignore
*.code-workspace
compile_commands.json
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Python (the analysis/ pipeline)
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/

# Ignore any auto-generated folders by Epic
Saved/
DerivedDataCache/

# Raw university-provided sensor/meteo data -- not cleared for publication.
# dataset_master_tfg.csv (the already-built dataset) stays tracked; only the
# raw inputs behind it are excluded. See REPRODUCE.md.
/analysis/data/pyranometers/
/analysis/data/solcast/
/analysis/data/meteo/
/analysis/data/meteo_utc_*.csv

# Optional: Ignore build tool files
*.xcodeproj
*.xcworkspace
*.sln

# Optional: Ignore specific files generated by VS
VSWorkspaceState.json
projectsettings.json
